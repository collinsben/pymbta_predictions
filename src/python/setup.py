from setuptools import setup, find_packages

setup(
    name='mbta_timer',
    author='Ben Collins',
    version='0.1.0',
    packages=find_packages(include=[
        'mbta_interface',
    ]),
    install_requires=[
        '',
    ],
)
